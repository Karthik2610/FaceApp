@model FaceApp.Models.ViewModels.LoginViewModel
@{
	ViewData["Title"] = "Login";
	Layout = "";
}

@using (Html.BeginForm("Login", "Account", FormMethod.Post, new { enctype = "multipart/form-data", id = "myLoginform", autocomplete = "Disabled", role = "form" }))
{
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Login - MemberTracker</title>


		<link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.css" />
		<link rel="stylesheet" href="~/css/site.css" />

		<link rel="stylesheet" href="~/css/style-1.css" />

		<script src="~/js/jquery-3.3.1.min.js"></script>
		<script src="~/js/bootstrap.js"></script>

	</head>
	<body class="login-body">

		<div class="container ">
			<div class="login_wrapper">
				<div class="login-form">
					<div class="login-title">
						<h3>Login</h3>
					</div>
					<div>
						@Html.LabelFor(m => m.Email, new { @class = "control-label" })
						<span style="color:#e85347;"> * </span>
						@Html.TextBoxFor(m => m.Email, new { placeholder = "Email", @class = "inputfile" })
						@Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger error" })
					</div>
					<div>
						@Html.LabelFor(m => m.Password, new { @class = "control-label" })
						<span style="color:#e85347;"> * </span>
						@Html.TextBoxFor(m => m.Password, new { @type="Password", placeholder = "Password", @class = "inputfile", @onchange = "OnchangePassword()" })
						@Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger error" })
					</div>
					<div class="loginBtn">
						<a href="@Url.Action("ForgotPassword", "Account")" class=""> forgot Password? </a>
					</div>
					<div class="loginBtn">
						@*<a href="" class="btn btn-login"> Login </a>*@
						<button type="submit" class="btn btn-login"> Login </button>
					</div>

				</div>
			</div>
		</div>

	</body>
}